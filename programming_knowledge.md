### OOP(Object Oriented Programming) 객체지향 프로그래밍.
- 부품코드(객체)를 먼저 만들고, 이 객체코드들을 조립해서 프로그램을 완성하는 프로그래밍 기법.
> 객체지향의 특징
>  1. 캡슐화
>    - 외부 객체는 객체 내부의 구조를 알지도 못하고 참조하는 객체가 제공한 메소드만 사용가능하다.
>   2. Inheritance(상속)
>    -  상위 객체의 속성, 메소드를 하위 객체에게 물려줘서 하위 객체가 사용할 수 있게 하는 것을 말한다.
>    - 재사용해서 새로운 객체를 만들기 때문에 반복된 코드의 중복을 줄여준다.
>    3. Polymorphism(다형성)
>    - 메소드나 클래스가 다양한 방법으로 동작하는 것을 의미한다.
>    - 동일한 조작 방법(호출 방식)으로 동작시키지만 동작방법이 다른 것을 의미한다.
>    -Overriding: 부모에게 상속받은 메소드를 재정의하여 다른 동작을 하도록 프로그래밍한다.
>    - Overloading : 동일한 메소드 명에 파라미터를 다르게 해서 다른 동작을 하도록 만들 수 있다.
>    - 같은 타입이지만 다양한 객체를 이용할 수 있는 성질.
>    - 하나의 타입에 여러 객체를 대입함으로써 다양한 기능을 이용할 수 있도록 해준다.
>    - 자바는 다형성을 위해 부모 클래스 또는 인터페이스의 타입변환을 허용한다. 

### CLASS(클래스)란 무엇?
- 메모리에 할당되지 않은 상태의 설계도같은 개념.
-  현실세계에서 객체의 속성과 동장을 모델링을 통해서 변수와 메소드로 정의한 것.
-
### Method and Class, what is difference??



### Object(객체)란 무엇?
- Class라는 설계도를 기반으로 실제 메모리에 할당하여 동작할 수 있도록 만든 Class의 instance객체를 조함하여 전체 프로그램을 완성하는 형태가 OOP
-  완성품에 대한 부품이다.
-  속성과 동작으로 구분된다.



### Abstraction는 무엇?
- 정의부만 선언만 되어 있는 함수를 가진 클래스를 상속받아 함수를 구현하는 형태.
### Abstraction Class는 무엇?
추상 메소드를 하나 이상 가지고 있는 클래스는 overriding이 필요한 추상 메소드가 있기 때문에 객체로 만들 수 없다.
### 접근제어자는 무엇?
- public : 어디서든 접근 가능.
- protected : 모든 하위 클래스에서 접근 가능.
- none : 같은 패키지의 모든 클래스에서 접근 가능.
- private: 같은 클래스의 모든 인스턴스에서 접근 가능. 하위 클래스에서 접근 불가능.
### JVM는 무엇?
- java Virtual Machine의 약자이다. 
- OS에 상관없이 동작하도록 OS 사이의 중계자 역할을 한다.

### Overloading는 무엇?
- 메소드 명이 동일하지만 매개변수 타입과 개수를 다르게 해서 선언하는 방식.
### Overriding는 무엇?
- 상속한 자식에서 부모의 클래스의 메소드를 제정의 해서 사용하는 방식.
### JSP(Java Server Pages)는 무엇?
- java 코드가 들어있는 HTML 페이지이다. servlet을 좀 더 쉽게 접근할 수 있도록 만들어져 있다.
- ### Servlet
- java를 써서 웹 페이지를 동적으로 생성하는 서버 측 프로그램이다. java코드 안에 HTML 요소를 가진다.
- 
### RESTFUL는 무엇?
- 해당 URL만 보아도 어떤 작업을 하는지 알 수 있도록 하나의 데이터가 하나의 URL을 갖도록 작업하는 방식이다.

> reference by [출처 블로그](https://blog.naver.com/indy9052/221354214398).
